{
  "name": "botpanel-saas",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "apps/client",
    "apps/admin"
  ],
  "scripts": {
    "start": "node server/index.js",
    "dev": "npm run dev -w @botpanel/server",
    "build": "npm run build -w @botpanel/client && npm run build -w @botpanel/admin",
    "check": "npm run check -w @botpanel/server",
    "test": "npm test -w @botpanel/server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
